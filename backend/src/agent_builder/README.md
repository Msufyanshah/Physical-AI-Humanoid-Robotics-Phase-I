This directory contains Agent Builder–generated (or mocked) runners.

In production, these runners are replaced by exported OpenAI Agent Builder workflows.
FastAPI interacts with agents ONLY through adapter functions.
    